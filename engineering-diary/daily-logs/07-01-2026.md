## 🧠 Plan for Today

-   Composer install spatie/laravel-permission and sanctum for authentication.
-   Configure Sanctum for API authentication.
-   Configure Spatie for role-based access control.
-   Create roles and permissions seeder and run it.
-   Configure middleware for the related API routes.

## 🛠 What I Worked On

-   Composer install spatie/laravel-permission and sanctum for authentication.
-   Configure Sanctum for API authentication.
-   Configure Spatie for role-based access control.
-   Create roles and permissions seeder and run it.
-   Configure middleware for the related API routes.

## 💭 Key Learnings

-   Composer install spatie/laravel-permission and sanctum for authentication.
    -   I learned that spatie/laravel-permission is a package that provides role-based access control for Laravel applications.
        -   It is a package that provides role-based access control for Laravel applications. It ease the process of managing roles and permissions, avoided devs from meticulously writing the code for role-based access control.
    -   I learned that sanctum is a package that provides API authentication for Laravel applications.
        -   It is a package that provides API authentication for Laravel applications. It ease the process of managing API authentication, avoided devs from meticulously writing the code for API authentication.
        -   I also learned that the personal access tokens that are generated upon user's logged in is meant to be deleted upon user's logout. This is to prevent the tokens from being manipulated and used by unauthorized attackers.
-   Configure Sanctum for API authentication.
    -   I learned that Sanctum is a package that provides API authentication for Laravel applications.
-   Configure Spatie for role-based access control.
    -   I learned that Spatie is a package that provides role-based access control for Laravel applications.
-   Create roles and permissions seeder and run it.
-   Configure middleware for the related API routes.

## 🧩 Bugs or Blockers

-   During running the seeder, I got an error: 
    ```
    Spatie\Permission\Exceptions\PermissionDoesNotExist 

    There is no permission named `product-view` for guard `web`.

    at vendor/spatie/laravel-permission/src/Exceptions/PermissionDoesNotExist.php:11
        7▕ class PermissionDoesNotExist extends InvalidArgumentException
        8▕ {
        9▕     public static function create(string $permissionName, ?string $guardName)
        10▕     {
    ➜  11▕         return new static(__('There is no permission named `:permission` for guard `:guard`.', [
        12▕             'permission' => $permissionName,
        13▕             'guard' => $guardName,
        14▕         ]));
        15▕     }

        +6 vendor frames 

    7   database/seeders/DatabaseSeeder.php:77
        Spatie\Permission\Models\Role::givePermissionTo()
        +37 vendor frames 

    45  artisan:16
        Illuminate\Foundation\Application::handleCommand(Object(Symfony\Component\Console\Input\ArgvInput))
    ```

    -   This happens as I need to run `php artisan cache:clear` to clear the cache first because the permissions and roles are cached. So, it thinks that the   new permissions and roles are not exists.
    -   I found a workaround to add `app()[\Spatie\Permission\PermissionRegistrar::class]->forgetCachedPermissions();` on the run function in the seeder file, above all seeder steps. But, I just commented it out as it is not needed. I will just run `php artisan cache:clear` before running the seeder instead.

## 🚀 Next Steps / Tomorrow

-   Ensure Sir Uzzair able to see the remote repo.
