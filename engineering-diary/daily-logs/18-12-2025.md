## 🧠 Plan for Today

-   [Part C - Controller & Routes](../../project-deliverables-checklist.md#part-c-controller--routes-25-marks)

## 🛠 What I Worked On

-   [Part C - Controller & Routes](../../project-deliverables-checklist.md#part-c-controller--routes-25-marks)
-   [Part D: API Testing with Bruno API Client](../../project-deliverables-checklist.md#part-d-api-testing-with-bruno-api-client-25-marks)

## 💭 Key Learnings

-   Hand's on practice of Laravel's controller and routes.
-   Hand's on practice of Laravel's built-in response code constants, instead of straight away typing the code number.
-   Hand's on practice of Laravel's validation.
-   Hand's on practice of Laravel's factory and discovered the FakerPHP library.
-   Hand's on practice of the difference of usage between `find()` and `findOrFail()` methods.
    -   `find()` returns `null` if not found.
    -   `findOrFail()` throws an exception if not found.
-   Learned about Laravel's `isEmpty()` method.
    -   Can be used to check if a collection is empty.
-   Learned about API testing using Bruno API Client.
    -   Discovered that the URL can be set as a variable in the Collection configuration.
-   A little bit about variable name good practices:
    -   Ensure that the variable name is descriptive and meaningful.
        -   This helps a lot in real-world development, where other developers can understand the code easily without reading the whole code. This improve's dev's productivity. I learned this from [this Instagram reels](https://www.instagram.com/reel/DSH1TJXjBSN/?igsh=bzl3cW5xcmxuY3U1).

## 🧩 Bugs or Blockers

-   Upon testing, I got error where the routes is not found, so I debug and found out that the routes is not registered at `bootstrap/app.php` file.
-   Upon testing, I got error where the routes is not found, so I debug and found out that the route's link is not correct 😮‍💨
-   During the [FakerPHP](https://fakerphp.org/formatters/) configs;
    -   I mistakenly used `word()` method instead of `words()` method. I should use `words()` method to generate multiple words.
    -   Because the product name is not people's name, I should use `words()` method instead of `name()` method.

## 🚀 Next Steps / Tomorrow

-   Overall check the project deliverables checklist and make sure I have completed all the tasks.
-   Maybe will find any other things that I can improve and learn.
